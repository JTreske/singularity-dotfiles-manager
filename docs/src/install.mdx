# Installation

The Singularity Dotfiles Manager is written in Rust.
You will need the Rust toolchain installed to build the application from source.
Use the following command to install the dotfiles manager directly from git:

```bash
cargo install --git https://github.com/JTreske/singularity-dotfiles-manager.git
```
